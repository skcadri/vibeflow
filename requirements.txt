# Core dependencies
numpy>=1.24.0
sounddevice>=0.4.6
PyQt6>=6.5.0
pynput>=1.7.6
pyyaml>=6.0

# MedASR dependencies
# Install PyTorch with CUDA first:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
librosa>=0.10.0

# Install transformers from git (requires >= 5.0.0 for MedASR)
git+https://github.com/huggingface/transformers.git@65dc261512cbdb1ee72b88ae5b222f2605aad8e5

# Packaging (for building .exe)
pyinstaller>=6.0.0
